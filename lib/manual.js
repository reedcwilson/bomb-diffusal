'use strict';

let wires = require('./wires'),
    button = require('./button'),
    keypad = require('./keypad'),
    onFirst = require('./on-first.js'),
    memory = require('./memory'),
    morse = require('./morse'),
    complicatedWires = require('./complicated-wires'),
    wireSequences = require('./wire-sequences.js'),
    maze = require('./maze'),
    knobs = require('./knobs');

module.exports = {
  wires,
  button,
  keypad,
  onFirst,
  memory,
  morse,
  complicatedWires,
  wireSequences,
  maze,
  knobs,
};
